{% include 'header' %}

import UIKit
import SwinjectStoryboard
import Reusable

// MARK: - {{ prefix }}{{ module_info.name }}ViewController

final class {{ prefix }}{{ module_info.name }}ViewController: UIViewController, StoryboardSceneBased {

	static let sceneStoryboard = SwinjectStoryboard.create(name: "{{ module_info.name }}Module", bundle: Bundle(for: {{ module_info.name }}ViewController.self), container: SwinjectStoryboard.defaultContainer) as UIStoryboard

    var output: {{ module_info.name }}ViewOutput!

    // MARK: Life cycle
    override func viewDidLoad() {
        super.viewDidLoad()
        output.viewIsReady()
    }
}

// MARK: - {{ prefix }}{{ module_info.name }}ViewInput

extension {{ prefix }}{{ module_info.name }}ViewController: {{ prefix }}{{ module_info.name }}ViewInput {
    
    func setupInitialState() {
        
    }
}
